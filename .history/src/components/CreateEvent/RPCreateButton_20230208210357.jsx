import Icon from "../common/Icon";


function RpCreateButton() {
  return (
    <div style={{ background: "aqua" }}>
      <button style={{backgroundImage: `${add}`}}>
      </button>
    </div>
  );
}

export default RpCreateButton;
